# Design

Technical design documents for significant features or cross-repo system architecture.

These are living documents — update them as the system evolves. For point-in-time decisions, use `decisions/` instead.
